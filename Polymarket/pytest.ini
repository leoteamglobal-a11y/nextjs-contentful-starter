[pytest]
pythonpath = src
testpaths = tests

# The live broker and runner are async, so their tests need pytest-asyncio
# (see requirements-dev.txt). Without `auto`, every `async def` test is
# skipped with a warning rather than failing — a suite that reports green
# while silently running none of them is worse than one that errors.
asyncio_mode = auto
