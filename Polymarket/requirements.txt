httpx>=0.27
websockets>=12.0

# Ed25519 request signing. Not optional any more: on Polymarket US the
# market data WebSocket is authenticated, so even recording a book needs to
# sign the handshake.
cryptography>=42.0
