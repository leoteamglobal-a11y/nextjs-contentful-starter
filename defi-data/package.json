{
  "name": "defi-data-ingester",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Etapa 1 de la plataforma de datos DeFi: ingesta append-only de DefiLlama Yields (apyBase/apyReward/TVL) + normalización + reporte de yield real.",
  "scripts": {
    "ingest": "tsx src/ingest.ts",
    "report": "tsx src/report.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
